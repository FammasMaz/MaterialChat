# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Enable parallel execution
org.gradle.parallel=true

# Enable daemon for faster builds
org.gradle.daemon=true

# Enable caching
org.gradle.caching=true

# Enable configuration caching
org.gradle.configuration-cache=true

# JVM settings for better performance
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# Use AndroidX
android.useAndroidX=true

# Enable non-transitive R classes for faster builds
android.nonTransitiveRClass=true

# Enable BuildConfig generation
android.defaults.buildfeatures.buildconfig=true

# Kotlin code style
kotlin.code.style=official

# Enable experimental Compose compiler metrics (optional for debugging)
# kotlin.compose.stabilityConfigurationPath=stability-config.txt
